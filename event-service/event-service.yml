spring:
  cloud:
    consul:
      port: 8500
      host: 172.23.16.1
      discovery:
        preferIpAddress: "true"
        instance-id: "${spring.cloud.client.hostname}:${spring.application.name}:${random.int[1,99999]}"
        health-check-url: "169.254.213.17/actuator/health"
management:
  endpoints:
    web:
      exposure:
        include: refresh,health
server:
  port: 8081
